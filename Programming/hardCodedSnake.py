import Instructions as i
import time
import random
import xorShifter as xs # xorShift random 0-255 generator # x31 is the input seed, x30 is the masked output x20 holds the 32-bit state, x21 is a temp/output register

